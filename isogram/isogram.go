package isogram

// IsIsogram return true if string is isogram
func IsIsogram(s string) bool{
	words := make(map[rune]int)
	duplicate := true
	for _, v := range s {
		_ = words
		_ = v
		
	}
	return duplicate
}